import { defineRecipe } from '@chakra-ui/react';

const LinkStyles = defineRecipe({
  base: {
    color: 'accent.red',
    textStyle: 'text.md',
    fontWeight: 400,
  },
});

export default LinkStyles;
